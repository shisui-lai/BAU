I have successfully implemented the requested translations in `src/renderer/src/locales/en.json`.

**1. "Block Info" Page Translations:**
Added the following to `blockInfoPage.parameters`:
*   `"使能簇状态1": "Enable Cluster Status 1"`
*   `"使能簇状态2": "Enable Cluster Status 2"`
*   `"切出簇状态1": "Cut-out Cluster Status 1"`
*   `"切出簇状态2": "Cut-out Cluster Status 2"`

**2. "Block Remote Adjust" (Block Config Param) Translations:**
Updated the section titles in `blockConfigParamPage.sections` to match your request:
*   `"portConfig": "System Port Configuration Parameters"` (was "System Port Config Parameters")
*   `"commonConfig": "Block System Basic Configuration"` (was "Block Basic Config")

**3. "Device Management" Page Translations:**
Also updated `deviceManagementPage.sections` to ensure consistency:
*   `"portConfig": "System Port Configuration Parameters"`
*   `"deviceCommonConfig": "Block System Basic Configuration"`

These changes ensure that the English translations appear correctly for both the new status fields and the modified tab names.
