# Reference 项目事件记录功能实现分析

## 一、整体架构

事件记录功能采用三层架构设计：

1. **渲染进程（Vue组件）**：负责UI交互、状态管理和用户操作
2. **主进程（IPC）**：负责消息转发、目录管理和事件协调
3. **子进程（Modbus任务）**：负责数据读取、解析和CSV文件导出

## 二、数据流架构

### 2.1 事件记录标志位数据读取流程

```
Modbus读取 (地址: 0x5730-0x573D, 15个寄存器)
  ↓
getEventTimeData() 解析函数
  ↓
生成 eventData 数组（6个字段）
  ↓
IPC发送 'FC04EventData' 事件
  ↓
渲染进程监听 'update-FC04EventData'
  ↓
更新 eventData.value（响应式数据）
```

**关键组件：**
- **读取位置**：`reference/modbus/handlers/read.js` - 周期性读取
- **解析函数**：`reference/modbus/handlers/utils.js:getEventTimeData()` - 数据解析
- **IPC监听**：`reference/renderer/src/views/Bcu/eventRecord/event.vue` - 数据接收

**数据字段（6个）：**
1. 事件记录版本号（ASCII编码，7个寄存器）
2. 事件记录存储数量（u16，单位：条）
3. 事件记录存储百分比（u16 / 100，单位：%）
4. 写事件记录开始位置（u32，2个寄存器组合）
5. 删除事件记录开始位置（u32，2个寄存器组合）
6. 等待删除事件记录数量（u32，2个寄存器组合）

### 2.2 事件记录导出流程

```
用户点击"导出"按钮
  ↓
confirmExport() - 显示确认对话框
  ↓
startExport() - 验证参数并启动导出
  ↓
IPC发送 'start-reading-data-event'
  ↓
主进程转发给子进程 'startReadEvent'
  ↓
子进程调用 startReadingEvent()
  ↓
循环读取事件记录（每次128个寄存器 = 1条记录）
  ↓
解析每条记录（时间戳、事件类型、参数、系统状态等）
  ↓
写入CSV文件（实时写入）
  ↓
发送进度更新 'readEventProgress'（每100条）
  ↓
完成后发送 'readEventCompleted'
  ↓
渲染进程更新UI状态
```

**关键参数：**
- `offsetRead`：起始偏移量（自动计算：总数 - 要读数量）
- `totalRead`：要读取的记录数（全部导出时为总数）
- `saveDir`：保存目录（使用默认目录，无需弹窗）

**文件结构：**
```
EventExports/
  └── Event_2025-04-23/          // 按日期分文件夹
      └── 192.168.1.100_20250423_153005.csv
```

## 三、核心功能模块

### 3.1 事件标志位读取

**功能描述：**
- 周期性读取设备的事件记录标志位寄存器（0x5730-0x573D）
- 解析并显示事件记录的存储状态信息
- 数据通过IPC事件 `update-FC04EventData` 实时更新

**数据解析：**
- 版本号：7个寄存器，ASCII编码转换为字符串
- 存储数量：直接读取u16值
- 存储百分比：u16值除以100，保留2位小数
- 位置信息：2个u16寄存器组合为u32值

**UI显示：**
- 使用DataTable组件显示6个字段
- 包含参数名、数值、单位三列
- 支持中英文国际化

### 3.2 事件记录导出

**功能描述：**
- 批量读取设备的事件记录数据
- 每条记录包含128个寄存器（256字节）
- 解析为可读格式并导出为CSV文件

**导出参数计算：**
```javascript
// 偏移量自动计算：从最新记录往前倒推
offsetRead = Math.max(0, 总存储数量 - 要读数量)

// 示例：
// 总存储数量 = 1000
// 要读数量 = 100
// offsetRead = 1000 - 100 = 900
// 结果：从第900条开始读，读到第1000条（最新100条）
```

**CSV文件结构：**
- **表头**：ID、时间戳、事件类型、参数1-4、系统状态、故障位、电压、电流、温度等（约80+列）
- **数据行**：
  - 普通事件（事件码 < 300）：写入解析后的可读数据
  - 参数写入事件（事件码 >= 300）：写入原始十六进制数据
- **CRC校验**：每条记录末尾包含CRC校验结果

**进度跟踪：**
- 每读取100条记录发送一次进度更新
- 通过IPC事件 `update-readEventProgress` 更新UI
- 使用ProgressBar组件显示进度条

**错误处理：**
- 读取超时：继续重试
- 致命错误：停止导出，发送错误通知
- 用户取消：清理状态，恢复心跳

### 3.3 事件记录删除

**功能描述：**
- 通过Modbus写入删除指令到设备
- 删除单位：16条记录为1包
- 支持部分删除和全部删除

**删除逻辑：**
```javascript
// 删除数量计算
删除记录数 = 用户输入的包数 × 16

// Modbus写入
地址：0xC00C
值：
  - 全部删除：0xFFFF
  - 部分删除：删除记录数（n × 16）
```

**验证规则：**
- 全部删除：复选框选中即可
- 部分删除：必须输入正整数（n > 0）

## 四、状态管理

### 4.1 ExportStore（导出状态管理）

**位置：** `reference/stores/eventStore.js`

**状态：**
- `isExporting`: 是否正在导出（boolean）
- `current`: 当前进度（number）
- `total`: 总数量（number）
- `percent`: 计算属性，导出百分比（computed）

**方法：**
- `start(totalCount)`: 开始导出，初始化状态
- `update(currentIndex)`: 更新当前进度
- `fail()`: 导出失败，重置状态

### 4.2 本地缓存

**localStorage键名：**
- `eventTime:events` - 事件标志位数据
- `eventTime:times` - 时间记录数据

**缓存策略：**
- 数据更新时自动保存（watch监听）
- 组件挂载时恢复缓存（onMounted）
- 支持数据持久化，页面刷新后恢复

## 五、IPC事件通信

### 5.1 渲染进程 → 主进程

| IPC事件 | 参数 | 用途 |
|---------|------|------|
| `start-reading-data-event` | `{offsetRead, totalRead, ip}` | 启动事件导出 |
| `cancelExport` | `{ip}` | 取消导出 |
| `choose-default-export-dir` | - | 选择导出目录（invoke） |
| `set-default-export-dir` | `dir` | 设置默认目录 |
| `get-default-export-dir` | - | 获取默认目录（invoke） |

### 5.2 主进程 → 渲染进程

| IPC事件 | 参数 | 用途 |
|---------|------|------|
| `update-FC04EventData` | `{ip, Arg: [...]}` | 更新事件标志位数据 |
| `export-started` | `saveDir` | 导出开始通知 |
| `export-completed` | `saveDir` | 导出完成通知 |
| `export-canceled` | `saveDir` | 导出取消通知 |
| `readEventErrorFromMain` | `{deviceIp, error}` | 导出错误通知 |
| `update-readEventProgress` | `{current, total}` | 更新导出进度 |

### 5.3 子进程 → 主进程

| API | 参数 | 用途 |
|-----|------|------|
| `readEventProgress` | `{deviceIp, current, total}` | 导出进度更新 |
| `readEventCompleted` | `currentSaveDir` | 导出完成 |
| `readEventCanceled` | `currentSaveDir` | 导出取消 |
| `readEventError` | `{deviceIp, error}` | 导出错误 |

## 六、UI交互逻辑

### 6.1 导出操作卡片

**功能区域：**
1. **导出路径**：显示默认目录，支持修改
2. **导出参数**：
   - 导出偏移量（只读，自动计算）
   - 导出数量（可输入）
   - 全部导出复选框
3. **导出按钮**：带进度条显示
4. **取消按钮**：导出过程中显示

**验证逻辑：**
- 事件记录数量为0时，不允许导出
- 全部导出时始终可用
- 部分导出时：必须输入正整数，且不超过总存储数量

### 6.2 删除操作卡片

**功能区域：**
1. **删除数量输入**：输入包数（n）
2. **自动计算显示**：n × 16 = 删除记录数
3. **全部删除复选框**
4. **删除按钮**

**验证逻辑：**
- 全部删除：复选框选中即可
- 部分删除：必须输入正整数（n > 0）

### 6.3 事件数据表格

**显示内容：**
- 事件记录版本号
- 事件记录存储数量
- 事件记录存储百分比
- 写事件记录开始位置
- 删除事件记录开始位置
- 等待删除事件记录数量

**表格列：**
- 参数（支持国际化）
- 数值
- 单位

## 七、关键技术点

### 7.1 环形缓冲区

事件记录使用环形缓冲区存储，当缓冲区满时，新记录会覆盖最旧的记录。

**偏移量计算逻辑：**
- 从最新记录往前倒推
- `offsetRead = 总数 - 要读数量`
- 确保读取的是最新的记录

### 7.2 批量读取策略

**读取方式：**
- 每次读取128个寄存器（1条记录）
- 从 `offsetRead` 开始，循环读取 `totalRead` 条
- 使用异步读取，避免阻塞

**性能优化：**
- 实时写入CSV，不占用大量内存
- 每100条记录发送一次进度更新，减少IPC通信频率

### 7.3 事件类型解析

**解析逻辑：**
- 根据事件码（寄存器7）查找事件类型定义
- 解析参数1-4（寄存器8-11）为可读格式
- 返回事件名称和格式化参数

**两种模式：**
- 普通事件（事件码 < 300）：解析为可读数据
- 参数写入事件（事件码 >= 300）：保留原始十六进制数据

### 7.4 CSV文件格式

**表头字段（约80+列）：**
- ID、时间戳、事件类型、参数1-4
- 系统状态、系统总状态位、故障位
- 电压、电流、温度、SOC、SOH、SOE
- 电量、容量、效率、SOP
- 接触器状态、DI/DO状态
- 配置信息、IP地址、MAC地址
- 版本号信息、CRC校验结果

**数据格式：**
- 时间戳：BCD解码后的格式化时间字符串
- 数值：带单位的格式化字符串（如 "3.5V", "25℃"）
- 状态：解析后的可读状态描述

## 八、错误处理机制

### 8.1 导出错误处理

**错误类型：**
- 读取超时：继续重试，不中断导出
- 致命错误：停止导出，发送错误通知，恢复心跳
- 用户取消：清理状态，关闭文件流，恢复心跳

**错误通知：**
- 通过IPC事件 `readEventErrorFromMain` 发送错误信息
- UI显示Toast错误提示
- 重置ExportStore状态

### 8.2 UI错误提示

**提示方式：**
- Toast通知：成功/失败/警告
- 进度条：显示导出进度
- 按钮状态：导出中禁用相关操作

## 九、默认导出目录管理

**目录管理：**
- 存储在全局变量 `DEFAULT_EXPORT_DIR`
- 默认值：`join(process.cwd(), 'EventExports')`
- 可通过IPC设置和获取
- 支持用户自定义目录

**目录结构：**
```
EventExports/
  └── Event_YYYY-MM-DD/          // 按日期分文件夹
      └── IP_YYYYMMDD_HHMMSS.csv // 按IP和时间命名
```

## 十、实现步骤建议

### 阶段一：基础UI和数据结构
1. 添加事件记录UI结构（导出卡片、删除卡片、数据表格）
2. 实现事件标志位数据读取和显示
3. 添加localStorage缓存支持

### 阶段二：导出功能
1. 实现导出参数计算逻辑
2. 实现IPC通信（渲染进程 ↔ 主进程 ↔ 子进程）
3. 实现CSV文件写入逻辑
4. 实现进度跟踪和UI更新

### 阶段三：删除功能
1. 实现删除参数计算逻辑
2. 实现Modbus写入删除指令
3. 实现删除确认对话框

### 阶段四：完善和优化
1. 错误处理和用户提示
2. 导出目录管理
3. 性能优化和测试

## 十一、注意事项

1. **数据一致性**：确保事件标志位数据和实际导出数据的一致性
2. **进度准确性**：进度更新频率要平衡用户体验和性能
3. **错误恢复**：导出失败后要正确清理状态，恢复心跳
4. **内存管理**：大量数据导出时要注意内存使用，使用流式写入
5. **用户体验**：提供清晰的进度反馈和错误提示
