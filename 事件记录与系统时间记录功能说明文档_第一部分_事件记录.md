# 事件记录与系统时间记录功能说明文档

## 第一部分：事件记录功能

### 1. 功能概述

事件记录功能用于读取、导出和删除设备存储的事件记录数据。设备通过MQTT协议提供事件记录相关的topic，上位机通过订阅和发布这些topic来实现事件记录的读取、导出和删除功能。

### 2. 核心组件

#### 2.1 文件结构

- **`src/renderer/src/views/Bau/eventRecord/event.vue`**：前端UI组件，负责用户交互和数据显示
- **`src/main/eventRecordExport.js`**：事件记录读取和导出核心逻辑，运行在MQTT子进程
- **`src/main/mqtt.js`**：MQTT消息处理和路由
- **`src/protocol/utils.js`**：事件记录数据解析函数
- **`src/protocol/eventRecordFormatter.js`**：事件记录字段格式化函数
- **`src/main/table.js`**：事件记录数据结构定义

#### 2.2 MQTT Topic

| Topic | 方向 | 说明 |
|-------|------|------|
| `bms/host/s2d/b${blockId}/event_record_flag_r` | 订阅 | 事件记录标志位数据 |
| `bms/host/s2d/b${blockId}/event_record_r` | 订阅 | 事件记录数据 |
| `bms/host/s2d/b${blockId}/event_record_r` | 发布 | 读取事件记录请求 |
| `bms/host/d2s/b${blockId}/clear_event_record_num` | 发布 | 删除事件记录命令 |
| `bms/bau/d2s/b${blockId}/clear_event_record_num` | 订阅 | 删除事件记录响应 |

### 3. 事件记录标志位（event_record_flag_r）

#### 3.1 数据结构

事件记录标志位包含以下字段：

- **上一次事件记录版本号**（str20）：ASCII编码的版本号字符串
- **事件记录存储数量**（u16）：设备中存储的事件记录总数
- **事件记录存储百分比**（u16, scale: 10000）：存储百分比，范围0-1000，单位0.01%
- **写事件记录开始位置**（u32）：当前写入事件记录的起始位置
- **删除事件记录开始位置**（u32）：当前删除事件记录的起始位置
- **等待删除事件记录数量**（u32）：等待删除的事件记录数量

#### 3.2 处理逻辑

1. **数据请求**：前端通过 `requestEventRecordFlag()` 函数每秒请求一次标志位数据
2. **数据解析**：`parseEventRecordFlagRAW()` 函数解析MQTT消息
3. **数据更新**：解析后的数据通过IPC发送到前端，更新UI显示

#### 3.3 关键参数说明

- **存储数量（StorageCount）**：用于计算导出偏移量
  - 导出偏移量 = 存储数量 - 要读数量
  - 如果导出全部，则偏移量为0

- **存储百分比（StoragePercent）**：用于显示存储状态
  - **当前项目处理**：`scale: 10`，实际值 = 寄存器值 / 10
    - 寄存器值 1000 → `1000 / 10 = 100` → 显示 "100.00%"
    - 寄存器值 500 → `500 / 10 = 50` → 显示 "50.00%"
  - **协议定义**：范围 0-1000，单位 0.1%
    - 寄存器值 1000 表示 100.0%（满存储）
    - 寄存器值 500 表示 50.0%（半存储）
  - **Reference项目处理**（参考）：`(params[8] / 100).toFixed(2)`
    - 寄存器值 1000 → `1000 / 100 = 10` → 显示 "10.00%"
    - **注意**：Reference项目与当前项目的处理方式不同，当前项目使用 `scale: 10` 以正确显示百分比

### 4. 事件记录读取（event_record_r）

#### 4.1 请求格式

**Topic**: `bms/host/s2d/b${blockId}/event_record_r`

**Payload**: 2字节uint16_t，小端序，表示要读取的事件记录偏移量（0~N）

**示例**：
```javascript
// 读取偏移量为25180的事件记录
const offsetPayload = Buffer.alloc(2)
offsetPayload.writeUInt16LE(25180, 0)
const recordPayloadHex = offsetPayload.toString('hex') // "5c62"
```

#### 4.2 响应格式

**Topic**: `bms/host/s2d/b${blockId}/event_record_r`

**Payload格式**：
- 前2字节：数据长度（uint16_t，小端序），通常为258（0x0102）
- 接下来2字节：事件记录偏移量（uint16_t，小端序），设备返回的RecordOffset
- 后续256字节：事件记录数据（128个寄存器 * 2字节）

**示例**：
```
0201 5d62 2500 1100 1200 0000 1100 4300 2300 8101 ...
│    │    │    │    │    │    │    │    │    │
│    │    │    │    │    │    │    │    │    └─ 事件类型（0x0181 = 385）
│    │    │    │    │    │    │    │    └─ 秒（0x0023 = 35 → 23）
│    │    │    │    │    │    │    └─ 分（0x0043 = 67 → 43）
│    │    │    │    │    │    └─ 时（0x0011 = 17 → 11）
│    │    │    │    │    └─ 周（0x0000 = 0 → 1）
│    │    │    │    └─ 日（0x0012 = 18 → 12）
│    │    │    └─ 月（0x0011 = 17 → 11）
│    │    │    └─ 年（0x0025 = 37 → 2025）
│    │    └─ RecordOffset（0x625d = 25181）
│    └─ DataLength（0x0102 = 258）
└─ 响应标识
```

#### 4.3 数据解析流程

1. **原始数据解析**（`parseEventRecordRAW`）：
   - 读取DataLength和RecordOffset
   - 提取256字节的事件记录数据
   - 使用 `parseByTable()` 函数按照 `EVENT_RECORD_R` 定义解析字段

2. **BCD时间解码**：
   - 事件记录的时间字段采用BCD编码
   - 字段顺序：年-月-日-周-时-分-秒（与系统时间记录的秒-分-时-周-日-月-年不同）
   - 解码后：
     - Year: 2000 + BCD值（0-99）→ 2000-2099
     - Month: BCD值（1-12）
     - Day: BCD值（1-31）
     - Hour: BCD值（0-23）
     - Minute: BCD值（0-59）
     - Second: BCD值（0-59）

3. **字段格式化**（`formatEventRecordField`）：
   - 事件类型：根据 `EVENT_TYPE_MAP` 转换为中文描述
   - 事件参数：根据 `EVENT_PARAM_MAPPING` 转换为中文描述
   - 状态字段：根据枚举映射转换为中文描述
   - 故障字段：根据位映射解析为故障名称列表
   - 数值字段：添加单位（V, A, ℃, %等）
   - 空值处理：使用 "/" 占位

#### 4.4 并行读取机制

##### 4.4.1 发送策略

- **发送间隔**：20ms（50 req/s）
- **滑动窗口**：最大同时等待的请求数量为200
- **发送流程**：
  1. 按20ms间隔发送请求，不等待响应
  2. 如果等待的请求数量达到200，暂停发送
  3. 当等待的请求数量低于200时，恢复发送

##### 4.4.2 匹配机制

- **等待器Map**：`eventRecordResponseWaiters`，key为recordIndex（请求偏移量）
- **匹配方式**：使用设备返回的RecordOffset直接匹配对应的等待器
- **缓存机制**：`eventRecordDataCache`，key为RecordOffset，存储解析后的数据

##### 4.4.3 进度监控

- **监控间隔**：100ms
- **完成条件**：`eventReadingSentCount === totalRead && eventReadingCompletedCount === totalRead`
- **进度更新**：通过IPC发送到前端，更新进度条

#### 4.5 关键参数说明

##### 4.5.1 offsetRead（读取起始偏移量）

**定义位置**：`src/renderer/src/views/Bau/eventRecord/event.vue` (第241-247行)

**计算公式**：
```javascript
const offsetRead = computed(() => {
  if (flagDownloadAll.value) {
    return 0  // 导出全部时，从0开始
  }
  const readCount = parseInt(valueRead.value) || 0
  return Math.max(0, storedCount.value - readCount)  // 存储数量 - 要读数量
})
```

**作用**：
- 确定从哪个偏移量开始读取事件记录
- 如果导出全部（`flagDownloadAll.value = true`），则 `offsetRead = 0`
- 如果导出部分，则从 `存储数量 - 要读数量` 开始读取（读取最新的N条记录）

**示例**：
- 存储数量 = 1000，要读数量 = 100 → `offsetRead = 1000 - 100 = 900`（读取第900-999条记录）
- 存储数量 = 1000，导出全部 → `offsetRead = 0`（读取第0-999条记录）

##### 4.5.2 totalRead（要读取的记录总数）

**定义位置**：`src/renderer/src/views/Bau/eventRecord/event.vue` (第249-253行)

**计算公式**：
```javascript
const totalRead = computed(() => {
  if (flagDownloadAll.value) {
    return storedCount.value  // 导出全部时，读取所有存储的记录
  }
  return parseInt(valueRead.value) || 0  // 导出部分时，读取用户指定的数量
})
```

**作用**：
- 确定要读取多少条事件记录
- 如果导出全部，则 `totalRead = 存储数量`
- 如果导出部分，则 `totalRead = 用户输入的数量`

**示例**：
- 存储数量 = 1000，导出全部 → `totalRead = 1000`
- 存储数量 = 1000，要读数量 = 100 → `totalRead = 100`

##### 4.5.3 recordIndex（请求偏移量）

**定义位置**：`src/main/eventRecordExport.js` (第261行)

**计算公式**：
```javascript
const recordIndex = offsetRead + sendIndex
```

**作用**：
- 每次发送请求时，计算当前要读取的事件记录偏移量
- `sendIndex` 从0开始递增，直到 `sendIndex < totalRead`
- 作为MQTT请求的payload发送给设备

**数据流向**：
1. **计算**：`recordIndex = offsetRead + sendIndex`（第261行）
2. **创建等待器**：`eventRecordResponseWaiters.set(recordIndex, { resolve, reject, requestedRecordIndex: recordIndex })`（第221行）
3. **发送请求**：将 `recordIndex` 编码为2字节小端序，作为payload发送（第214-216行）
4. **匹配响应**：设备返回的 `RecordOffset` 应该等于 `recordIndex`（用户保证它们一致）

**示例**：
- `offsetRead = 900`, `sendIndex = 0` → `recordIndex = 900`（请求第900条记录）
- `offsetRead = 900`, `sendIndex = 1` → `recordIndex = 901`（请求第901条记录）
- `offsetRead = 900`, `sendIndex = 99` → `recordIndex = 999`（请求第999条记录）

##### 4.5.4 RecordOffset（设备返回的事件记录偏移量）

**定义位置**：`src/protocol/utils.js` (第2747行)

**解析位置**：`src/protocol/utils.js` (第2747行)
```javascript
const recordOffset = buf.readUInt16LE(2)  // 从响应的第2-3字节读取
```

**作用**：
- 设备在响应中返回的事件记录偏移量，标识这条记录在设备中的实际位置
- 用于匹配请求和响应（作为 `eventRecordResponseWaiters` 的key）
- 用于缓存数据（作为 `eventRecordDataCache` 的key）

**数据流向**：
1. **设备响应**：设备在响应的第2-3字节返回 `RecordOffset`（小端序）
2. **解析**：`parseEventRecordRAW()` 函数解析 `RecordOffset`（`src/protocol/utils.js` 第2747行）
3. **匹配等待器**：`processEventRecordResponse()` 函数使用 `RecordOffset` 查找对应的等待器（`src/main/eventRecordExport.js` 第100行）
4. **缓存数据**：使用 `RecordOffset` 作为key，将解析后的数据存入 `eventRecordDataCache`（第124行）

**重要说明**：
- **用户保证**：`RecordOffset` 与请求的 `recordIndex` 完全一致（一对一映射）
- **匹配策略**：直接使用 `RecordOffset` 作为key匹配等待器，无需FIFO队列或差值检测
- **缓存key**：`eventRecordDataCache.set(RecordOffset, { recordIndex: RecordOffset, baseConfig, data, ... })`

**示例**：
- 请求 `recordIndex = 900` → 设备返回 `RecordOffset = 900`
- 请求 `recordIndex = 901` → 设备返回 `RecordOffset = 901`

### 5. 事件记录导出（CSV）

#### 5.1 导出流程

1. **数据读取完成**：所有请求的响应都已收到
2. **数据排序**：按recordIndex排序
3. **CSV生成**：一次性生成CSV文件（所有数据行一次性写入）

#### 5.2 数据排序逻辑

**代码位置**：`src/main/eventRecordExport.js` (第577-579行)

**排序时机**：在CSV生成函数 `generateEventRecordCSV()` 中，所有数据读取完成后进行排序

**排序方式**：
```javascript
// 按recordIndex排序，写入数据行
const sortedRecords = Array.from(eventRecordDataCache.entries())
  .sort((a, b) => a[0] - b[0])  // 按recordIndex（Map的key）升序排序
```

**排序原因**：
- 设备响应是乱序到达的（并行发送，响应可能不按请求顺序返回）
- 需要按 `recordIndex` 升序排序，确保CSV文件中的记录按时间顺序排列

**排序结果**：
- `sortedRecords` 是一个数组，每个元素是 `[recordIndex, recordData]` 的格式
- `recordIndex` 从 `offsetRead` 开始，到 `offsetRead + totalRead - 1` 结束
- 例如：`offsetRead = 900`, `totalRead = 100` → 排序后的数组包含 `[900, ...], [901, ...], ..., [999, ...]`

#### 5.3 CSV生成方式

**代码位置**：`src/main/eventRecordExport.js` (第407-711行)

**生成方式**：**一次性生成**（所有数据行一次性写入文件）

**详细流程**：

1. **创建文件流**（第438行）：
   ```javascript
   const csvStream = fs.createWriteStream(outputFile, { encoding: 'utf8', flags: 'w' })
   ```

2. **写入BOM**（第441行）：
   ```javascript
   csvStream.write('\uFEFF')  // UTF-8 BOM，确保Excel正确识别中文
   ```

3. **生成表头**（第443-575行）：
   - 遍历 `EVENT_RECORD_R` 表定义，生成表头数组 `csvHeaders`
   - 处理时间字段合并、故障字段合并等特殊逻辑
   - 写入表头行：`csvStream.write(escapedHeaders.join(',') + '\n')`（第575行）

4. **生成数据行**（第577-705行）：
   - **遍历排序后的记录**：`for (const [recordIndex, recordData] of sortedRecords)`（第584行）
   - **逐行生成**：对每条记录，生成一个CSV行（包含ID和所有字段值）
   - **逐行写入**：`csvStream.write(row.join(',') + '\n')`（第704行）
   - **注意**：虽然是一次性生成，但数据行是逐条处理的（循环中逐条写入）

5. **关闭文件流**（第708行）：
   ```javascript
   csvStream.end()
   ```

**关键点**：
- **不是逐条写入文件**：所有数据在内存中排序完成后，一次性写入文件流
- **不是分批写入**：所有数据行在一个循环中连续写入
- **内存占用**：排序后的 `sortedRecords` 数组会占用内存，但数据行是流式写入的（不会一次性将所有行拼接成字符串）

**性能考虑**：
- 对于大量记录（如1000条），排序操作的时间复杂度为 O(n log n)
- 数据行写入是流式的，不会一次性占用大量内存
- 如果记录数量非常大（如10000条以上），可能需要考虑分批处理

### 6. 完整数据流向

#### 6.1 用户点击导出按钮

**代码位置**：`src/renderer/src/views/Bau/eventRecord/event.vue` (第650-700行)

**触发函数**：`handleExport()`

**执行流程**：

1. **计算参数**（第651-653行）：
   ```javascript
   const offsetRead = computed(() => { ... })  // 计算读取起始偏移量
   const totalRead = computed(() => { ... })   // 计算要读取的记录总数
   ```

2. **发送IPC消息**（第655-660行）：
   ```javascript
   window.electron.ipcRenderer.send('start-reading-event', {
     blockId: blockInfo.blockNumber,
     offsetRead: offsetRead.value,
     totalRead: totalRead.value,
     exportDir: exportDir.value
   })
   ```
   - **消息类型**：`start-reading-event`
   - **消息数据**：`{ blockId, offsetRead, totalRead, exportDir }`
   - **目标进程**：主进程（Main Process）

#### 6.2 主进程接收IPC消息

**代码位置**：`src/main/index.js`

**处理逻辑**：
- 主进程监听 `start-reading-event` IPC消息
- 将消息转发到MQTT子进程：`mqttProcess.send({ type: 'start-reading-event', data: { blockId, offsetRead, totalRead, exportDir } })`

#### 6.3 MQTT子进程接收消息

**代码位置**：`src/main/mqtt.js`

**处理逻辑**（第XXX行）：
```javascript
process.on('message', (msg) => {
  if (msg.type === 'start-reading-event') {
    const { blockId, offsetRead, totalRead, exportDir } = msg.data
    startReadingEvent(blockId, offsetRead, totalRead, exportDir, client)
  }
})
```

**函数调用**：`startReadingEvent()`（定义在 `src/main/eventRecordExport.js`）

#### 6.4 开始读取事件记录

**代码位置**：`src/main/eventRecordExport.js` (第147-315行)

**函数签名**：`startReadingEvent(blockId, offsetRead, totalRead, exportDir, client)`

**执行流程**：

1. **初始化状态变量**（第157-168行）：
   ```javascript
   isReadingEvent = true
   eventReadingBlockId = blockId
   eventReadingOffset = offsetRead
   eventReadingTotal = totalRead
   eventReadingCurrent = 0
   eventReadingCanceled = false
   eventReadingError = false
   eventReadingSentCount = 0
   eventReadingCompletedCount = 0
   eventRecordDataCache.clear()  // 清空缓存
   eventRecordResponseWaiters.clear()  // 清空等待器
   currentSaveDir = exportDir
   ```

2. **创建Promise**（第184行）：
   ```javascript
   return new Promise((resolve, reject) => {
     // 发送逻辑
   })
   ```

3. **启动进度更新定时器**（第189-203行）：
   ```javascript
   const progressTimer = setInterval(() => {
     const current = eventRecordDataCache.size
     process.send({
       type: 'readEventProgress',
       data: { blockId, current, total: eventReadingTotal }
     })
   }, 100)  // 每100ms更新一次进度
   ```

4. **定义发送请求函数**（第206-250行）：
   ```javascript
   const sendRequest = (recordIndex) => {
     // 1. 计算recordIndex = offsetRead + sendIndex
     // 2. 创建等待器：eventRecordResponseWaiters.set(recordIndex, { resolve, reject })
     // 3. 编码payload：Buffer.alloc(2); offsetPayload.writeUInt16LE(recordIndex, 0)
     // 4. 发送MQTT请求：client.publish(recordTopic, payloadBuf)
   }
   ```

5. **定义调度函数**（第252-270行）：
   ```javascript
   const scheduleNextSend = () => {
     if (sendIndex < totalRead) {
       const recordIndex = offsetRead + sendIndex
       sendRequest(recordIndex)
       sendIndex++
       if (sendIndex < totalRead) {
         sendTimer = setTimeout(scheduleNextSend, SEND_INTERVAL_MS)  // 20ms后发送下一个
       }
     }
   }
   ```

6. **启动发送**（第273行）：
   ```javascript
   scheduleNextSend()  // 立即发送第一个请求
   ```

7. **监控完成状态**（第276-295行）：
   ```javascript
   const monitorInterval = setInterval(() => {
     if (eventReadingSentCount === totalRead && eventReadingCompletedCount === totalRead) {
       // 所有请求已发送且所有响应已收到
       clearInterval(monitorInterval)
       resolve()  // Promise完成
     }
   }, 100)
   ```

#### 6.5 设备响应处理

**代码位置**：`src/main/mqtt.js`

**MQTT消息处理**（第XXX行）：
```javascript
// 订阅 topic: bms/host/s2d/b${blockId}/event_record_r
// 收到消息后调用：
processEventRecordResponse(responseData, client)
```

**函数调用**：`processEventRecordResponse()`（定义在 `src/main/eventRecordExport.js`）

#### 6.6 处理事件记录响应

**代码位置**：`src/main/eventRecordExport.js` (第38-137行)

**函数签名**：`processEventRecordResponse(responseData, client)`

**执行流程**：

1. **提取RecordOffset**（第70行）：
   ```javascript
   const { RecordOffset, baseConfig, data, result, rawRegisters, rawBuffer } = responseData
   ```

2. **检查错误响应**（第72-97行）：
   ```javascript
   if (data?.error || result?.error) {
     const waiter = eventRecordResponseWaiters.get(RecordOffset)
     if (waiter) {
       eventRecordResponseWaiters.delete(RecordOffset)
       eventReadingCompletedCount++
       waiter.reject(new Error(data?.message || `参数错误 (code: ${data?.code})`))
     }
   }
   ```

3. **处理成功响应**（第99-135行）：
   ```javascript
   else {
     const waiter = eventRecordResponseWaiters.get(RecordOffset)
     if (waiter) {
       eventRecordResponseWaiters.delete(RecordOffset)
       eventReadingCompletedCount++
       
       // 缓存数据
       eventRecordDataCache.set(RecordOffset, {
         recordIndex: RecordOffset,
         baseConfig,
         data,
         rawRegisters,
         rawBuffer,
         deviceRecordOffset: RecordOffset
       })
       
       // 通知等待的Promise
       waiter.resolve({ recordIndex: RecordOffset, baseConfig, data })
     }
   }
   ```

**数据解析**：`responseData` 由 `parseEventRecordRAW()` 函数生成（`src/protocol/utils.js` 第2727行）

#### 6.7 解析事件记录数据

**代码位置**：`src/protocol/utils.js` (第2727-2840行)

**函数签名**：`parseEventRecordRAW(payload)`

**执行流程**：

1. **读取数据长度和RecordOffset**（第2746-2747行）：
   ```javascript
   const dataLen = buf.readUInt16LE(0)  // 前2字节：数据长度
   const recordOffset = buf.readUInt16LE(2)  // 接下来2字节：RecordOffset
   ```

2. **提取事件记录数据**（第2748行）：
   ```javascript
   const recordDataBuf = buf.slice(4)  // 后续256字节：事件记录数据
   ```

3. **解析字段**（第2773行）：
   ```javascript
   const { baseConfig: parsedBaseConfig } = parseByTable(view, EVENT_RECORD_R)
   ```

4. **合并数据**（第2776-2780行）：
   ```javascript
   let baseConfig = {
     DataLength: dataLen,
     RecordOffset: recordOffset,
     ...parsedBaseConfig  // 包含所有解析后的字段值
   }
   ```

5. **BCD时间解码**（第2782-2800行）：
   ```javascript
   // 对Year, Month, Day, Hour, Minute, Second进行BCD解码
   baseConfig.Year = parseBCD(baseConfig.Year)
   baseConfig.Month = parseBCD(baseConfig.Month)
   // ...
   ```

6. **返回解析结果**（第2810-2820行）：
   ```javascript
   return {
     error: false,
     baseConfig,
     data: {},
     result: {},
     rawRegisters,
     rawBuffer: rawBufferCopy
   }
   ```

#### 6.8 所有数据读取完成

**代码位置**：`src/main/eventRecordExport.js` (第296-315行)

**完成条件**：`eventReadingSentCount === totalRead && eventReadingCompletedCount === totalRead`

**执行流程**：

1. **Promise完成**（第292行）：
   ```javascript
   resolve()  // Promise完成，进入then回调
   ```

2. **调用stopReadingEvent**（第298行）：
   ```javascript
   stopReadingEvent(blockId, eventReadingCanceled, eventReadingError)
   ```

#### 6.9 停止读取并生成CSV

**代码位置**：`src/main/eventRecordExport.js` (第323-395行)

**函数签名**：`stopReadingEvent(blockId, wasCanceled, hasError)`

**执行流程**：

1. **检查是否有缓存数据**（第347行）：
   ```javascript
   const cachedCount = eventRecordDataCache.size
   ```

2. **生成CSV文件**（第380-385行）：
   ```javascript
   if (cachedCount > 0) {
     try {
       generateEventRecordCSV(savedBlockId, currentSaveDir, cachedCount)
     } catch (csvError) {
       console.error(`CSV文件生成失败: ${csvError.message}`)
     }
   }
   ```

3. **发送完成通知**（第387-395行）：
   ```javascript
   process.send({
     type: 'readEventCompleted',
     data: {
       blockId: savedBlockId,
       saveDir: currentSaveDir,
       cachedCount: cachedCount,
       totalRequested: eventReadingTotal,
       partial: isPartial
     }
   })
   ```

#### 6.10 生成CSV文件

**代码位置**：`src/main/eventRecordExport.js` (第407-711行)

**函数签名**：`generateEventRecordCSV(blockId, saveDir, recordCount)`

**执行流程**：

1. **创建目录和文件**（第413-433行）：
   ```javascript
   const eventFolderPath = path.join(saveDir, `Event_${dateOnly}`)
   fs.mkdirSync(eventFolderPath, { recursive: true })
   const outputFile = path.join(eventFolderPath, `b${blockId}_${timestamp}.csv`)
   const csvStream = fs.createWriteStream(outputFile, { encoding: 'utf8', flags: 'w' })
   ```

2. **写入BOM**（第441行）：
   ```javascript
   csvStream.write('\uFEFF')
   ```

3. **生成表头**（第443-575行）：
   - 遍历 `EVENT_RECORD_R` 表定义
   - 处理时间字段合并、故障字段合并
   - 生成 `csvHeaders` 数组
   - 写入表头行

4. **排序数据**（第577-579行）：
   ```javascript
   const sortedRecords = Array.from(eventRecordDataCache.entries())
     .sort((a, b) => a[0] - b[0])  // 按recordIndex排序
   ```

5. **生成数据行**（第584-705行）：
   ```javascript
   for (const [recordIndex, recordData] of sortedRecords) {
     const baseConfig = recordData.baseConfig || {}
     const row = [rowIndex++]  // ID从1开始
     
     // 遍历字段，格式化并添加到row
     for (let i = 0; i < csvFieldKeys.length; i++) {
       const fieldKey = csvFieldKeys[i]
       const value = baseConfig[fieldKey]
       let cellValue = formatEventRecordField(fieldKey, value, baseConfig, fieldDef)
       // CSV转义
       const escapedValue = cellValue.replace(/"/g, '""')
       row.push(`"${escapedValue}"`)
     }
     
     // 写入CSV行
     csvStream.write(row.join(',') + '\n')
   }
   ```

6. **关闭文件流**（第708行）：
   ```javascript
   csvStream.end()
   ```

#### 6.11 字段格式化

**代码位置**：`src/protocol/eventRecordFormatter.js`

**函数调用**：`formatEventRecordField(fieldKey, value, baseConfig, fieldDef)`

**执行流程**：

1. **处理空值**（第XXX行）：
   ```javascript
   if (value === undefined || value === null || value === '') {
     return parseNull(value)
   }
   ```

2. **处理时间戳字段**（第XXX行）：
   ```javascript
   if (fieldKey === 'Timestamp') {
     // 合并Year, Month, Day, Hour, Minute, Second
   }
   ```

3. **处理状态字段**（第XXX行）：
   ```javascript
   if (['EventType', 'StackChargeDischargeStatus', ...].includes(fieldKey)) {
     return formatStatusField(fieldKey, value)
   }
   ```

4. **处理事件参数**（第XXX行）：
   ```javascript
   if (fieldKey.startsWith('Param')) {
     return formatEventParam(baseConfig.EventType, fieldKey, value, baseConfig)
   }
   ```

5. **处理数值字段**（第XXX行）：
   ```javascript
   // 添加单位（V, A, ℃, %等）
   // 处理小数精度（scale: 10 → 1位小数）
   ```

#### 6.12 前端接收完成通知

**代码位置**：`src/renderer/src/views/Bau/eventRecord/event.vue` (第797-820行)

**IPC监听**：
```javascript
window.electron.ipcRenderer.on('export-completed', (_, data) => {
  console.log('[EventRecord] 导出完成:', data)
  // 显示成功提示
  toast.add({
    severity: 'success',
    summary: t('eventTime.toast.exportSuccess'),
    detail: t('eventTime.toast.exportSuccessDetail', { count: data.cachedCount }),
    life: 3000
  })
})
```

#### 6.13 数据流向总结

**完整流程**：
```
用户点击导出
  ↓
前端计算 offsetRead, totalRead
  ↓
发送IPC消息到主进程
  ↓
主进程转发到MQTT子进程
  ↓
MQTT子进程调用 startReadingEvent()
  ↓
并行发送MQTT请求（每20ms一个）
  ↓
设备响应（乱序到达）
  ↓
parseEventRecordRAW() 解析响应
  ↓
processEventRecordResponse() 处理响应
  ↓
缓存到 eventRecordDataCache
  ↓
所有响应接收完成
  ↓
stopReadingEvent() 调用
  ↓
generateEventRecordCSV() 生成CSV
  ↓
排序数据（按recordIndex）
  ↓
格式化字段（formatEventRecordField）
  ↓
写入CSV文件
  ↓
发送完成通知到前端
  ↓
前端显示成功提示
```

### 7. CSV文件格式

#### 7.1 文件命名

```
EventExports/Event_YYYY-MM-DD/b{blockId}_YYYY-MM-DD_HH-mm-ss.csv
```

**示例**：
```
EventExports/Event_2025-11-12/b1_2025-11-12_11-39-26.csv
```

#### 7.2 文件内容

- **BOM**：UTF-8 BOM（`\uFEFF`），确保Excel正确识别中文
- **表头**：ID + 字段标签（从 `EVENT_RECORD_R` 定义中提取）
- **数据行**：
  - ID：从1开始的序号
  - 时间戳：合并为 `YYYY-M-D-HH:mm:ss` 格式
  - 其他字段：按表头顺序排列

#### 7.3 字段处理

1. **时间戳合并**：
   - 将Year, Month, Day, Hour, Minute, Second合并为单个"时间戳"列
   - 格式：`YYYY-M-D-HH:mm:ss`
   - 如果时间字段不完整，使用 "/" 占位

2. **合并字段**：
   - **簇汇总模拟量三级告警**：按严重程度分组（严重故障、一般故障、轻微故障）
   - **簇汇总硬件故障**：合并所有故障字（Word1-6）
   - **堆硬件故障**：合并所有故障字（Word1-6）
   - 合并逻辑：过滤掉"无故障"、空字符串和"/"，用逗号连接故障信息；如果所有字段都无故障，显示"无故障"

3. **字段格式化**：
   - 事件类型：转换为中文描述
   - 事件参数：根据事件类型和参数索引转换为中文描述
   - 状态字段：根据枚举映射转换为中文描述
   - 故障字段：根据位映射解析为故障名称列表
   - 数值字段：添加单位，保留小数位（scale: 10 表示保留1位小数）
   - 空值处理：使用 "/" 占位

4. **CSV转义**：
   - 双引号转义为两个双引号
   - 字段值用双引号包裹

#### 5.3 导出目录

- **默认目录**：`{项目根目录}/EventExports`
- **用户自定义**：可以通过UI选择导出目录
- **目录结构**：按日期创建子目录 `Event_YYYY-MM-DD`

### 6. 事件记录删除

#### 6.1 删除命令

**Topic**: `bms/host/d2s/b${blockId}/clear_event_record_num`

**Payload**: 2字节uint16_t，小端序
- `0x0000`：不删除
- `0xFFFF`：删除全部
- 其他值：删除的数量

**示例**：
```javascript
// 删除16条记录
const deletePayload = Buffer.alloc(2)
deletePayload.writeUInt16LE(16, 0)
const deletePayloadHex = deletePayload.toString('hex') // "1000"
```

#### 6.2 删除响应

**Topic**: `bms/bau/d2s/b${blockId}/clear_event_record_num`

**响应格式**：参考其他堆遥控命令的响应格式

#### 6.3 删除流程

1. **用户输入**：输入删除包数（每个包16条记录）
2. **计算删除数量**：`deleteCount = n * 16`
3. **发送删除命令**：通过 `mqttPublish` 发送删除命令
4. **等待响应**：监听 `CLEAR_EVENT_RECORD_NUM` IPC事件
5. **显示结果**：根据响应结果显示成功或失败提示

### 7. 错误处理

#### 7.1 错误类型

1. **参数错误**（Code 228）：请求的偏移量无效
2. **超时错误**：请求超时未收到响应
3. **网络错误**：MQTT连接断开
4. **数据错误**：解析失败或数据格式错误

#### 7.2 错误处理策略

1. **参数错误**：跳过该记录，继续读取其他记录
2. **超时错误**：记录超时数量，继续读取其他记录
3. **网络错误**：停止读取，发送错误通知
4. **数据错误**：记录错误数量，继续处理其他记录

#### 7.3 完成条件

- **成功**：成功读取的记录数 > 0
- **部分成功**：成功读取的记录数 < 请求的记录数，但 > 0
- **失败**：成功读取的记录数 = 0

### 8. 状态管理

#### 8.1 全局状态变量

- `isReadingEvent`：是否正在读取事件记录
- `eventReadingBlockId`：当前读取的堆ID
- `eventReadingOffset`：当前读取的起始偏移量
- `eventReadingTotal`：当前读取的总记录数
- `eventReadingCurrent`：当前已读取的记录数
- `eventReadingCanceled`：是否已取消
- `eventReadingError`：是否已发送错误通知
- `eventRecordDataCache`：事件记录数据缓存
- `eventRecordResponseWaiters`：事件记录响应等待器Map

#### 8.2 状态流转

1. **开始读取**：设置 `isReadingEvent = true`，初始化状态变量
2. **读取中**：更新 `eventReadingCurrent`，缓存数据
3. **完成读取**：设置 `isReadingEvent = false`，生成CSV文件
4. **取消读取**：设置 `eventReadingCanceled = true`，清理资源
5. **错误处理**：设置 `eventReadingError = true`，发送错误通知

### 9. IPC通信

#### 9.1 主进程 → 渲染进程

- `readEventProgress`：读取进度更新
- `readEventCompleted`：读取完成
- `readEventCanceled`：读取取消
- `readEventError`：读取错误
- `CLEAR_EVENT_RECORD_NUM`：删除事件记录响应

#### 9.2 渲染进程 → 主进程

- `read-event-record`：开始读取事件记录
- `cancel-event-record`：取消读取事件记录
- `delete-event-record`：删除事件记录（已废弃，改用 `mqttPublish`）

### 10. 关键函数说明

#### 10.1 `startReadingEvent(blockId, offsetRead, totalRead, exportDir, client)`

开始读取事件记录。

**参数**：
- `blockId`：堆ID
- `offsetRead`：读取起始偏移量
- `totalRead`：要读取的记录总数
- `exportDir`：导出目录路径
- `client`：MQTT客户端

**流程**：
1. 初始化状态变量
2. 按20ms间隔发送请求
3. 监控读取进度
4. 所有请求完成后生成CSV文件

#### 10.2 `processEventRecordResponse(responseData, blockId, client)`

处理事件记录响应。

**参数**：
- `responseData`：响应数据对象，包含RecordOffset, baseConfig, data, result等
- `blockId`：堆ID
- `client`：MQTT客户端

**流程**：
1. 检测响应顺序（用于统计）
2. 如果错误，拒绝对应的Promise
3. 如果成功，匹配等待器，缓存数据，解析Promise

#### 10.3 `generateEventRecordCSV(eventRecordDataCache, exportDir, blockId)`

生成事件记录CSV文件。

**参数**：
- `eventRecordDataCache`：事件记录数据缓存
- `exportDir`：导出目录路径
- `blockId`：堆ID

**流程**：
1. 创建导出目录
2. 生成CSV文件名
3. 生成表头
4. 按recordIndex排序数据
5. 格式化并写入数据行
6. 关闭文件流

#### 10.4 `formatEventRecordField(fieldKey, value, baseConfig, fieldDef)`

格式化事件记录字段。

**参数**：
- `fieldKey`：字段键
- `value`：字段值
- `baseConfig`：完整的基础配置对象
- `fieldDef`：字段定义

**返回值**：格式化后的字符串

**处理逻辑**：
1. 空值处理：返回 "/"
2. 事件类型：根据 `EVENT_TYPE_MAP` 转换
3. 事件参数：根据 `EVENT_PARAM_MAPPING` 转换
4. 状态字段：根据枚举映射转换
5. 故障字段：根据位映射解析
6. 数值字段：添加单位，保留小数位
7. 二进制字段：转换为二进制字符串（如簇标志位）

### 11. 数据结构定义

#### 11.1 EVENT_RECORD_R

事件记录数据结构定义，包含以下字段：

- **时间字段**：Year, Month, Day, Week, Hour, Minute, Second
- **事件类型**：EventType
- **事件参数**：Param1, Param2, Param3, Param4
- **堆信息**：StackVoltage, StackCurrent, StackSOC, StackTemp等
- **故障字段**：ClusterAnalogAlarm_*, ClusterHardwareFault_*, StackHardwareFault_*
- **其他字段**：EventRecordVersion, BOOTVersion, SoftwareVersion等

#### 11.2 EVENT_TYPE_MAP

事件类型映射表，将事件类型ID映射为中文描述。

**示例**：
```javascript
{
  100: '系统上电',
  101: '系统下电',
  385: '下设绝缘电阻检测指令',
  387: '下设绝缘电阻检测指令'
}
```

#### 11.3 EVENT_PARAM_MAPPING

事件参数映射表，将事件类型和参数索引映射为解析函数。

**示例**：
```javascript
{
  385: {
    1: { label: '参数1', parse: (value) => value === 1 ? '使能' : '禁用' },
    2: { label: '参数2', parse: (value) => value === 7 ? '7天' : `${value}天` }
  }
}
```

### 12. 性能优化

#### 12.1 并行发送

- **发送间隔**：20ms（50 req/s）
- **滑动窗口**：最大200个并发请求
- **优势**：大幅提升读取速度，从10条/秒提升到80条/秒

#### 12.2 数据缓存

- **缓存策略**：使用Map缓存解析后的数据，key为RecordOffset
- **内存管理**：读取完成后立即生成CSV文件，释放内存

#### 12.3 进度更新

- **更新频率**：100ms更新一次
- **批量更新**：避免频繁IPC通信，提升性能

### 13. 注意事项

1. **RecordOffset匹配**：设备返回的RecordOffset必须与请求的recordIndex一致，否则无法匹配
2. **BCD时间解码**：事件记录的时间字段采用BCD编码，需要正确解码
3. **CSV转义**：字段值中的双引号和逗号需要正确转义
4. **空值处理**：空值使用 "/" 占位，确保CSV格式正确
5. **合并字段**：故障字段需要合并显示，避免CSV列数过多
6. **文件编码**：CSV文件使用UTF-8 BOM编码，确保Excel正确显示中文

---

## 第二部分：系统时间与记录功能

### 1. 功能概述

系统时间与记录功能用于显示设备的系统时间信息和系统运行记录。设备通过MQTT协议提供系统时间记录相关的topic，上位机通过订阅这些topic来获取和显示系统时间、系统启动次数以及最近3次系统启动的记录信息。

### 2. 核心组件

#### 2.1 文件结构

- **`src/renderer/src/views/Bau/eventRecord/event.vue`**：前端UI组件，负责用户交互和数据显示
- **`src/main/mqtt.js`**：MQTT消息处理和路由
- **`src/protocol/utils.js`**：系统时间记录数据解析函数
- **`src/main/table.js`**：系统时间记录数据结构定义

#### 2.2 MQTT Topic

| Topic | 方向 | 说明 |
|-------|------|------|
| `bms/host/s2d/b${blockId}/sys_run_time_r` | 订阅 | 系统时间记录数据 |

### 3. 系统时间记录（sys_run_time_r）

#### 3.1 数据结构

系统时间记录包含以下字段：

- **系统当前时间**（7个寄存器，BCD编码：秒-分-时-周-日-月-年）
- **系统启动次数**（1个寄存器，uint16_t）
- **第1次系统记录**（21个寄存器）
  - 系统启动时间（7个寄存器，BCD编码：秒-分-时-周-日-月-年）
  - 系统停止时间（7个寄存器，BCD编码：秒-分-时-周-日-月-年）
  - 系统运行时间（2个寄存器，uint32_t，单位：分钟）
  - 周期任务堆栈大小（2个寄存器，uint32_t，单位：字节）
  - 系统堆栈空间（2个寄存器，uint32_t，单位：字节）
  - 系统堆栈最小空间（2个寄存器，uint32_t，单位：字节）
  - 预留（10个寄存器）
- **第2次系统记录**（21个寄存器）- 结构同第1次
- **第3次系统记录**（21个寄存器）- 结构同第1次
- **预留**（16个寄存器）

**总计**：120个寄存器（240字节）

#### 3.2 数据请求

**Topic**: `bms/host/s2d/b${blockId}/sys_run_time_r`

**Payload**: `'ff'`（固定值）

**请求频率**：每秒一次（周期性请求）

#### 3.3 响应格式

**Topic**: `bms/host/s2d/b${blockId}/sys_run_time_r`

**Payload格式**：
- 成功响应：前2字节为数据长度（uint16_t，小端序），通常为240（0x00F0），后续240字节为系统时间记录数据
- 失败响应：1字节错误码

#### 3.4 数据解析流程

1. **原始数据解析**（`parseSysRunTimeRAW`）：
   - 读取数据长度
   - 使用 `parseByTable()` 函数按照 `SYS_RUN_TIME_R` 定义解析字段

2. **BCD时间解码**（`processSysRunTimeData`）：
   - 系统时间记录的时间字段采用BCD编码
   - 字段顺序：秒-分-时-周-日-月-年（与事件记录的年-月-日-周-时-分-秒不同）
   - 解码后：
     - Year: 2000 + BCD值（0-99）→ 2000-2099
     - Month: BCD值（1-12）
     - Day: BCD值（1-31）
     - Week: BCD值（1-53，ISO周数，不输出但需要校验）
     - Hour: BCD值（0-23）
     - Minute: BCD值（0-59）
     - Second: BCD值（0-59）
   - 格式化：将7个时间字段合并为单个字符串 `YYYY-M-D-HH:mm:ss`

3. **32位字段组合**：
   - **运行时间**：`RunTime_Low | (RunTime_High << 16)` → 转换为 "xx天xx小时xx分钟"
   - **周期任务堆栈大小**：`PeriodicStack_Low | (PeriodicStack_High << 16)` → 转换为 "xx.xKb"
   - **系统堆栈空间**：`SystemStack_Low | (SystemStack_High << 16)` → 转换为 "xx.xKb"
   - **系统堆栈最小空间**：`SystemStackMin_Low | (SystemStackMin_High << 16)` → 转换为 "xx.xKb"

4. **字段分组**（`groupByClass`）：
   - 按 `class` 属性分组
   - 自动跳过 `undefined` 值和 `skip` 类型的字段
   - 更新label，使其更符合显示需求

#### 3.5 关键参数说明

- **系统当前时间**：设备的当前系统时间，格式化为 `YYYY-M-D-HH:mm:ss`
- **系统启动次数**：设备的总启动次数，用于循环缓冲区计算
- **系统启动时间**：每次系统启动的时间，格式化为 `YYYY-M-D-HH:mm:ss`
- **系统停止时间**：每次系统停止的时间，格式化为 `YYYY-M-D-HH:mm:ss`
- **系统运行时间**：每次系统运行的时长，转换为 "xx天xx小时xx分钟"
- **周期任务堆栈大小**：周期任务的堆栈使用情况，转换为 "xx.xKb"
- **系统堆栈空间**：系统堆栈的使用情况，转换为 "xx.xKb"
- **系统堆栈最小空间**：系统堆栈的最小可用空间，转换为 "xx.xKb"

### 4. 循环缓冲区逻辑

#### 4.1 循环缓冲区原理

系统时间记录使用循环缓冲区（Circular Buffer）来存储最近3次系统启动的记录。当系统启动次数超过3次时，新的记录会覆盖最旧的记录。

**示例**：
- 启动次数1：记录存储在位置0
- 启动次数2：记录存储在位置1
- 启动次数3：记录存储在位置2
- 启动次数4：记录存储在位置0（覆盖第1次记录）
- 启动次数5：记录存储在位置1（覆盖第2次记录）
- 启动次数6：记录存储在位置2（覆盖第3次记录）
- 以此类推...

#### 4.2 循环缓冲区计算

**最新记录位置**：
```javascript
const newestIdx = (bootNum - 1) % total  // total = 3
```

**最旧记录位置**：
```javascript
const oldestIdx = (newestIdx + 1) % total
```

**显示顺序**：从最旧到最新
```javascript
for (let i = 0; i < total; i++) {
  const idx = (oldestIdx + i) % total
  rec.push(timeGroups.value[idx])
}
```

#### 4.3 显示逻辑

1. **启动次数 <= 3**：
   - 直接从位置0开始顺序显示
   - 显示实际记录数（1-3条）

2. **启动次数 > 3**：
   - 使用循环缓冲区逻辑
   - 计算最新记录位置：`(bootNum - 1) % 3`
   - 计算最旧记录位置：`(newestIdx + 1) % 3`
   - 从最旧到最新排序，显示3条记录

**示例**：
- 启动次数 = 5
  - 最新记录位置：`(5 - 1) % 3 = 1`
  - 最旧记录位置：`(1 + 1) % 3 = 2`
  - 显示顺序：位置2（第3次）→ 位置0（第4次）→ 位置1（第5次）

### 5. 前端显示

#### 5.1 UI组件结构

1. **实时时间显示**：
   - 显示当前系统时间（实时更新）

2. **系统当前时间**：
   - 显示设备的系统当前时间
   - 格式：`YYYY-M-D-HH:mm:ss`

3. **系统启动次数**：
   - 显示设备的总启动次数
   - 格式：数字

4. **系统记录分组**：
   - 显示最近3次系统启动的记录
   - 每条记录包含：
     - 系统启动时间
     - 系统停止时间
     - 系统运行时间
     - 周期任务堆栈大小
     - 系统堆栈空间
     - 系统堆栈最小空间

#### 5.2 数据格式化

1. **时间格式化**：
   - 格式：`YYYY-M-D-HH:mm:ss`
   - 无效时间处理：显示为灰色或红色

2. **运行时间格式化**：
   - 格式：`xx天xx小时xx分钟`
   - 如果为0，显示 "0分钟"

3. **堆栈大小格式化**：
   - 格式：`xx.xKb`
   - 如果为0，显示为灰色

#### 5.3 样式处理

1. **无效时间**：
   - 检查：`/^2000-0?-?[01]-0?-?[01]/` 或 `/0000年00月第00周/`
   - 样式：红色（`text-red-500`）

2. **运行时间或堆栈相关字段**：
   - 检查：值大于0
   - 样式：白色（`text-default`）

3. **零值或无效值**：
   - 检查：值为0或无效
   - 样式：灰色（`text-gray-400`）

### 6. 数据更新机制

#### 6.1 周期性请求

- **请求频率**：每秒一次
- **请求函数**：`requestSysRunTime()`
- **请求Payload**：`'ff'`（固定值）

#### 6.2 数据更新流程

1. **发送请求**：通过 `mqttPublish` 发送请求
2. **接收响应**：MQTT子进程接收响应
3. **数据解析**：`parseSysRunTimeRAW` 解析数据
4. **数据处理**：`processSysRunTimeData` 处理数据
5. **数据分组**：`groupByClass` 分组数据
6. **IPC通信**：通过IPC发送到前端
7. **UI更新**：前端更新UI显示

#### 6.3 数据缓存

- **缓存位置**：前端Vue组件的 `timeElements` ref
- **更新策略**：每次收到新数据时更新
- **数据格式**：数组，每个元素包含 `label` 和 `value`

### 7. 关键函数说明

#### 7.1 `parseSysRunTimeRAW(payload)`

解析系统时间记录原始数据。

**参数**：
- `payload`：MQTT消息payload（Buffer或hex字符串）

**返回值**：
- 成功：`{ error: false, baseConfig, data }`
- 失败：`{ error: true, baseConfig: {}, data: { code, message } }`

**流程**：
1. 读取数据长度
2. 使用 `parseByTable` 解析字段
3. 调用 `processSysRunTimeData` 处理数据
4. 返回处理后的数据

#### 7.2 `processSysRunTimeData(baseConfig, schema)`

处理系统时间记录数据。

**参数**：
- `baseConfig`：解析后的基础配置对象
- `schema`：数据结构定义（SYS_RUN_TIME_R）

**返回值**：分组后的数据数组

**流程**：
1. BCD时间解码和格式化
2. 32位字段组合
3. 字段分组（`groupByClass`）
4. 更新label，使其更符合显示需求
5. 返回分组后的数据

#### 7.3 `formatSystemTime(params)`

格式化系统时间。

**参数**：
- `params`：时间参数数组，顺序为：秒-分-时-周-日-月-年

**返回值**：格式化后的时间字符串 `YYYY-M-D-HH:mm:ss`

**流程**：
1. BCD解码每个时间字段
2. 应用范围校验
3. 格式化时间字符串
4. 返回格式化后的字符串

#### 7.4 `convertMinutesToDayHour(minutes)`

将分钟转换为 "xx天xx小时xx分钟"。

**参数**：
- `minutes`：分钟数

**返回值**：格式化后的字符串

**示例**：
- `1440` → "1天"
- `1500` → "1天1小时"
- `90` → "1小时30分钟"
- `0` → "0分钟"

### 8. 数据结构定义

#### 8.1 SYS_RUN_TIME_R

系统时间记录数据结构定义，包含以下字段：

- **系统当前时间**：CurrentTime_Second, CurrentTime_Minute, CurrentTime_Hour, CurrentTime_Week, CurrentTime_Day, CurrentTime_Month, CurrentTime_Year
- **系统启动次数**：BootCount
- **第1次系统记录**：Boot1_StartTime_*, Boot1_StopTime_*, Boot1_RunTime_*, Boot1_PeriodicStack_*, Boot1_SystemStack_*, Boot1_SystemStackMin_*
- **第2次系统记录**：Boot2_*（结构同第1次）
- **第3次系统记录**：Boot3_*（结构同第1次）

#### 8.2 字段类型

- **时间字段**：u16，BCD编码
- **启动次数**：u16
- **运行时间**：u32（2个u16寄存器，Low和High）
- **堆栈大小**：u32（2个u16寄存器，Low和High）

### 9. 注意事项

1. **BCD时间解码**：系统时间记录的时间字段采用BCD编码，字段顺序为秒-分-时-周-日-月-年（与事件记录不同）
2. **循环缓冲区**：当系统启动次数超过3次时，使用循环缓冲区逻辑计算显示顺序
3. **32位字段组合**：运行时间和堆栈大小字段需要将Low和High寄存器组合为32位整数
4. **时间格式化**：时间字段需要合并为单个字符串，格式为 `YYYY-M-D-HH:mm:ss`
5. **无效时间处理**：无效时间（如2000-1-1-00:00:00）需要特殊处理，显示为灰色或红色
6. **数据更新频率**：系统时间记录每秒更新一次，确保时间显示的实时性

### 10. 与事件记录的区别

| 特性 | 事件记录 | 系统时间记录 |
|------|---------|-------------|
| **时间字段顺序** | 年-月-日-周-时-分-秒 | 秒-分-时-周-日-月-年 |
| **数据长度** | 256字节（128个寄存器） | 240字节（120个寄存器） |
| **请求方式** | 按偏移量请求 | 固定Payload（'ff'） |
| **更新频率** | 按需请求 | 每秒一次 |
| **数据存储** | 循环缓冲区（事件记录） | 循环缓冲区（系统记录） |
| **用途** | 记录系统事件 | 记录系统运行信息 |

---

## 总结

本文档详细说明了事件记录和系统时间记录功能的实现逻辑，包括：

1. **事件记录功能**：
   - 事件记录标志位读取
   - 事件记录读取（并行读取机制）
   - 事件记录导出（CSV格式）
   - 事件记录删除
   - 错误处理和状态管理

2. **系统时间记录功能**：
   - 系统时间记录读取
   - 循环缓冲区逻辑
   - 数据格式化和显示
   - 周期性数据更新

两个功能都通过MQTT协议实现，使用不同的topic和数据格式，但都遵循相同的数据解析和处理流程。

